import React from 'react';

const About = (props) => {
  return (
    <div>
      <h2>{props.title}</h2>
      <p>Node, Express, and React Template with Typescript</p>
    </div>
  );
};

export default About;
